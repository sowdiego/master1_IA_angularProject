# master1_IA_angularProject
Simuler une application backend (API REST avec json server) db.json 2. Créer une application frontend Angular nommée EtudiantApp  et mettre en place les opérations de CRUD
